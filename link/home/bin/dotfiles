#!/usr/bin/env bash
set -euo pipefail
if [[ ${SET_X:-} ]]; then
  set -x
fi
IFS=$'\n\t'

# Tries to be as idempotent as possible
# Doesn't handle deletions

function d_bootstrap() {
  local candidate candidate_path vim_path='' editor_path=''
  for candidate in code-insiders code mvim gvim vim; do
    if ! candidate_path=$(command -v $candidate); then
      continue
    fi

    if [[ ! $vim_path && $candidate == *'vim' ]]; then
      vim_path=$candidate_path
    fi

    if [[ ! $editor_path ]]; then
      editor_path=$candidate_path
    fi
  done

  if [[ ! $editor_path ]]; then
    echo >&2 'No editor found'
    return 1
  fi

  if [[ ! $vim_path ]]; then
    echo >&2 'No Vim found'
    return 1
  fi

  EDITOR=$editor_path
  BUNDLER_EDITOR=$editor_path
  VIM_PATH=$vim_path
  VISUAL=$VIM_PATH # Needed for `crontab -e` (`code --wait` doesn't work)

  local vars=(
    EDITOR
    BUNDLER_EDITOR
    VIM_PATH
    VISUAL
    DOTFILES
    DOTFILES_SHARED
    DOTFILES_INCLUDES
    DOTFILES_CUSTOM
  )

  local bootstrap='' var
  for var in "${vars[@]}"; do
    bootstrap+="$(declare -p "$var")"$'\n'
  done
  echo_eval 'printf %q > %q/.dotfiles_bootstrap.sh' "$bootstrap" "$HOME_DIR"
}

function d_edit() {
  local target
  if [[ $EDITOR == */code ]]; then
    target=$(find "$DOTFILES" -name '*.code-workspace' -maxdepth 1 -print -quit)
  fi

  # shellcheck disable=SC1091
  . "$DOTFILES_INCLUDES"/lib/editing.sh
  open_with_editor "${target:-${DOTFILES:?}}"
}

function d_setup() {
  local plug="$HOME_DIR/.vim/autoload/plug.vim" \
        vim_plug_installed=true
  if [[ ! -f $plug ]]; then
    echo_eval 'curl -fLo %q --create-dirs \
      https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim ' "$plug"
    vim_plug_installed=false
  fi

  d_sync all $vim_plug_installed

  d_setup_omz_custom
  js_install_globals
}

function d_setup_omz_custom() {
  if ! command -v omz > /dev/null 2>&1; then
    return 0
  fi

  local cmd='omz_install_custom' cmd_args=()
  if [[ $OSTYPE == darwin* ]]; then
    cmd="iterm_tab %q $cmd"
    cmd_args+=("$PWD")
  fi
  echo_eval "$cmd" "${cmd_args[@]}"
}

# TODO: Add archive feature: unarchive when the app is installed again
function d_sync() {
  local type=${1:-all} vim_plug_installed=${2:-true}

  if [[ $type == all || $type == shared ]]; then
    _d_setup_shared_links
  fi

  if [[ $type == all || $type == custom ]]; then
    _d_setup_custom_links
  fi

  d_vim_setup false "$vim_plug_installed"
}

function d_update() {
  DRY_RUN='' echo_eval 'cd %q' "$DOTFILES" || return
  if [[ -v TERMUX_VERSION ]]; then
    # shellcheck disable=SC1091
    . "$DOTFILES_INCLUDES"/lib/termux.sh
    echo_eval 'termux_prepare_ssh'
  fi
  for_each_dir 'git pull --prune --quiet'
  echo_eval 'd_sync'
}

function d_import() {
  local dotfile_type=${1:-''}
  if ! _d_validate_type "$dotfile_type" || [[ $# -lt 2 ]]; then
    _d_log_error "$USAGE_BASE ${FUNCNAME[0]} <shared|custom> <path 1> [<path 2> ...]" 1
    return 1
  fi

  shift

  DOTFILE_TYPE=$dotfile_type _d_process_paths "$@"
}

function d_revert_import() {
  REVERT_IMPORT=1 _d_process_paths "$@"
}

function d_revert_import_and_remove() {
  REVERT_IMPORT=1 REMOVE=1 _d_process_paths "$@"
}

function _d_process_paths() {
  for item_path in "$@"; do
    if [ $# -gt 1 ]; then
      color >&2 'white-bold' "---> $item_path"
    fi

    if [[ ${REVERT_IMPORT:-} ]]; then
      _d_revert_path "$item_path" || true
    else
      _d_import_path "${DOTFILE_TYPE:-}" "$item_path" || true
    fi
  done
}

function _d_import_path() {
  local dotfile_type=${1:?} item_path=${2:?}

  if [[ ! -e $item_path ]]; then
    _d_log_error 'Does not exist'
    return 1
  fi

  if [[ -L $item_path ]]; then
    _d_log_error 'Is already a symlink'
    return 1
  fi

  if _d_should_ignore "$item_path"; then
    _d_log_error 'Ignored'
    return 1
  fi

  local dotfile_dir
  case "$dotfile_type" in
    shared) dotfile_dir=$DOTFILES_SHARED ;;
    custom) dotfile_dir=$DOTFILES_CUSTOM ;;
  esac

  local dir_type relative_path
  _d_dir_type_and_relative_path "$item_path" dir_type relative_path
  local path_in_dotfiles=$dotfile_dir/link/$dir_type/${relative_path#/}

  # Git doesn't allow sym-linked .gitignore files, so we link .git/info/exclude instead
  # But .git is a special name that is always ignored, so we rename it within dotfiles
  # https://stackoverflow.com/q/72729555/25475271
  local is_local_git=false
  if [[ $item_path == .git/* || $item_path == */.git/* ]]; then
    is_local_git=true
    path_in_dotfiles=${path_in_dotfiles//\/.git\//\/.git.linked\/}
  fi

  local relative_dir=${relative_path%/*}

  if [[ $is_local_git ]]; then
    relative_dir=${relative_dir//\/.git\//\/.git.linked\/}
  fi

  if [[ $relative_dir == "$relative_path" ]]; then
    relative_dir=''
  fi

  _d_mk_dir "$dotfile_dir/link/$dir_type/$relative_dir"
  _d_echo_eval_with_sudo 'mv %q %q' "$item_path" "$path_in_dotfiles"
  IGNORES_CHECKED=1 _d_link_file "$path_in_dotfiles" "$(dirname "$item_path")"

  return 0
}

function _d_link_platform() {
  local dir=$1 platform_file platformless_link platform_suffix
  platform_suffix=$(_d_platform_suffix)
  while IFS= read -r -d '' platform_file; do
    platformless_link=${platform_file/_"$platform_suffix"/}
    if _d_need_to_link "$platform_file" "$platformless_link"; then
      echo_eval 'ln -s %q %q' "$platform_file" "$platformless_link"
    fi
  done < <(find "$dir" -name "*$PLATFORM_PREFIX$platform_suffix*" -print0)
}

function _d_platform_suffix() {
  if [[ $OSTYPE == darwin* ]]; then
    echo 'mac'
  elif [[ -v TERMUX_VERSION ]]; then
    echo 'termux'
  elif [[ $OSTYPE == linux* ]]; then
    echo 'linux'
  fi
}

function _d_revert_path() {
  local orig_path=${1:?}

  if [[ -L $orig_path ]]; then
    local dotfile_path
    if ! dotfile_path=$(readlink -f "$orig_path"); then
      _d_log_error "Target: $dotfile_path does not exist"

      local orig_dir
      orig_dir=$(dirname "$orig_path")
      if prompt "Remove broken links in $orig_dir?"; then
        remove_broken_links "$orig_dir"
      fi

      return 1
    fi

    local dotfiles
    if in_dir "$dotfile_path" "$DOTFILES_SHARED"; then
      dotfiles=$DOTFILES_SHARED
    fi
    if in_dir "$dotfile_path" "$DOTFILES_CUSTOM"; then
      dotfiles=$DOTFILES_CUSTOM
    fi

    if [[ ! $dotfiles ]]; then
      _d_log_error "Target: $dotfile_path is not in dotfiles"
      return 1
    fi

  elif [[ -e $orig_path ]] && _d_should_hard_link "$orig_path"; then
    local dir_type relative_path
    _d_dir_type_and_relative_path "$orig_path" dir_type relative_path

    local dotfile_path=$DOTFILES_SHARED/link/$dir_type/$relative_path
    dotfiles=$DOTFILES_SHARED
    if [[ ! -e $dotfile_path ]]; then
      dotfile_path=$DOTFILES_CUSTOM/link/$dir_type/$relative_path
      dotfiles=$DOTFILES_CUSTOM
      if [[ ! -e $dotfile_path  ]]; then
        _d_log_error 'Does not exist in dotfiles'
        return 1
      fi
    fi

    # Since both are real files, treat the original as the latest
    if [[ ! ${REMOVE:-''} ]]; then
      _d_echo_eval_with_sudo 'rm %q' "$dotfile_path"
    fi

  else
    _d_log_error 'Neither a symlink nor a hard link'
    return 1
  fi

  local cmd
  if [[ ${REMOVE:-''} ]]; then
    cmd='rm'
  else
    cmd='mv'
  fi
  _d_echo_eval_with_sudo "$cmd %q %q" "$dotfile_path" "$orig_path"

  # Remove empty directories in the dotfiles folder recursively until the link root
  local dotfile_dir
  dotfile_dir=$(dirname "$dotfile_path")
  local dotfiles_home_dir=$dotfiles/link/home
  local dotfiles_root_dir=$dotfiles/link/root
  while [[ $(find "$dotfile_dir") == "$dotfile_dir" ]]; do
    if [[ $dotfile_dir == "$dotfiles_home_dir" || $dotfile_dir == "$dotfiles_root_dir" ]]; then
      break
    fi
    echo_eval 'rmdir %q' "$dotfile_dir"
    dotfile_dir=$(dirname "$dotfile_dir")
  done

  return 0
}

function _d_setup_shared_links() {
  _d_link_platform "$DOTFILES_SHARED"
  _d_setup_links "$DOTFILES_SHARED"
}

function _d_setup_custom_links() {
  if [[ -d $DOTFILES_CUSTOM ]]; then
    _d_link_platform "$DOTFILES_CUSTOM"
    _d_setup_links "$DOTFILES_CUSTOM"
  fi
}

function _d_setup_links() {
  local dotfiles_path=${1:?} dir_type
  for dir_type in home root; do
    _d_start_link_recursive "$dotfiles_path"/link/$dir_type $dir_type
  done
}

function _d_start_link_recursive() {
  local dir=$1 dir_type=$2
  if [[ ! -d $dir ]]; then
    return 0
  fi
  _d_link_recursive "$dir" "$dir" "$dir_type"
}

function _d_link_recursive() {
  RECURSION_LEVEL=${RECURSION_LEVEL:-0}
  RECURSION_LEVEL=$((RECURSION_LEVEL + 1))
  if [[ $RECURSION_LEVEL -gt 10 ]]; then
    _d_log_error 'Too many levels of recursion'
    return 1
  fi

  local current_source_dir=$1 source_base=$2 dir_type=$3 relative_base target_base target_base_name target_dir
  relative_base=$(relative_to "$source_base" "$current_source_dir")

  if [[ -L $current_source_dir ]]; then
    current_source_dir=$(readlink -f "$current_source_dir")
  fi

  case "$relative_base" in
    .) relative_base='' ;;
    */.git.linked*) relative_base=${relative_base/.git.linked/.git} ;;
  esac

  case "$dir_type" in
    home)
      target_base=$HOME_DIR
      target_base_name=$HOME_DIR
      ;;
    root)
      target_base=$ROOT_DIR
      target_base_name=${ROOT_DIR%/}
      ;;
    *)
      _d_log_error "Unknown link type: $dir_type"
      return 1
      ;;
  esac

  target_dir=$target_base
  if [[ $relative_base ]]; then
    target_dir=$target_base_name/$relative_base
  fi

  if [[ $current_source_dir != "$source_base" ]]; then
    _d_mk_dir "$target_dir"
  fi

  local dir_source
  # shellcheck disable=SC2044
  for dir_source in $(_d_find_immediate_children "$current_source_dir" d); do
    RECURSION_LEVEL=$RECURSION_LEVEL _d_link_recursive "$dir_source" "$source_base" "$dir_type"
  done

  local file_source
  # shellcheck disable=SC2044
  for file_source in $(_d_find_immediate_children "$current_source_dir" f); do
    _d_link_file "$file_source" "$target_dir"
  done

  local link_source
  # shellcheck disable=SC2044
  for link_source in $(_d_find_immediate_children "$current_source_dir" l); do
    if [[ -d $(readlink -f "$link_source") ]]; then
      RECURSION_LEVEL=$RECURSION_LEVEL _d_link_recursive "$link_source" "$source_base" "$dir_type"
    else
      _d_link_file "$link_source" "$target_dir"
    fi
  done
}

function _d_dir_type_and_relative_path() {
  local item_path=$1 item_relative_to_home
  local -n dir_type_var=$2 relative_path_var=$3
  item_relative_to_home=$(relative_to "$HOME_DIR" "$item_path")
  # shellcheck disable=SC2034
  if [[ $item_relative_to_home == '../'* ]]; then
    dir_type_var=root
    relative_path_var=$(relative_to "$ROOT_DIR" "$item_path")
  else
    dir_type_var=home
    relative_path_var=$item_relative_to_home
  fi
}

function _d_mk_dir() {
  local dir=$1
  if [[ -d $dir || $dir == "$ROOT_DIR" || $dir == "$HOME_DIR" ]]; then
    return 0
  fi

  if [[ -L $dir || -f $dir ]]; then
    _d_backup "$dir"
  fi

  _d_echo_eval_with_sudo 'mkdir -p %q' "$dir"
}

function _d_find_immediate_children() {
  local dir=$1 find_type=$2
  find "$dir" -mindepth 1 -maxdepth 1 -type "$find_type"
}

function _d_link_file() {
  local source=$1 target_dir=$2

  if [[ ! ${IGNORES_CHECKED:-} ]] && _d_should_ignore "$source"; then
    return
  fi

  if [[ $source == *$PLATFORM_PREFIX* ]]; then
    return
  fi

  local source_base
  source_base=$(basename "$source")

  local hard_link=false
  if _d_should_hard_link "$source"; then
    if [[ -v TERMUX_VERSION ]]; then
      return
    fi
    hard_link=true
  fi

  local target=$target_dir/$source_base

  if ! _d_need_to_link "$source" "$target" "$hard_link"; then
    return 0
  fi

  _d_backup "$target" "$hard_link" "$source"

  local link_arg=' -s' comment=''
  if [[ $hard_link == true ]]; then
    link_arg=' -f'
    comment=' # Hard link'
  fi

  _d_echo_eval_with_sudo 'ln%s %q %q%s' "$link_arg" "$source" "$target" "$comment"
}

function _d_need_to_link() {
  local source=$1 target=$2 hard_link=${3:-false}

  if [[ ! -e $target ]]; then
    return 0
  fi

  if [[ $hard_link == false ]]; then
    [[ $(readlink -f "$target") != $(readlink -f "$source") ]]
  else
    if [[ -e $source ]] && same_inode "$target" "$source"; then
      return 1
    fi
  fi
}

# macOS no longer accepts symlinks to plist files https://github.com/lra/mackup/issues/1924.
# So for now, we use hard links. Notes:
# - They are prone to being replaced by actual files after a system or app upgrade
# - Some change very often, so you can exclude them
function _d_should_hard_link() {
  [[ $1 == *.plist ]]
}

# TODO: Replace with real ignore check
function _d_should_ignore() {
  [[ $1 == *.DS_Store || $1 == *.swp ]]
}

function _d_validate_type() {
  local type=$1
  if [[ $type != custom && $type != shared ]]; then
    local msg='Unknown dotfile type'
    if [[ $type ]]; then
      msg+=": $type"
    fi
    _d_log_error "$msg"
    return 1
  fi
}

function _d_backup() {
  local target=$1 hard_link=${2:-false} source=${3:-}

  if [[ ! -L $target && ! -f $target && ! -d $target || $target == "$ROOT_DIR" || $target == "$HOME_DIR" ]]; then
    return 0
  fi

  if [[ -L $target && ! -e $target ]]; then
    _d_echo_eval_with_sudo 'rm %q # Broken link' "$target"
    return
  fi

  if [[ $hard_link == true ]]; then
    if [[ $source ]]; then
      _d_echo_eval_with_sudo 'cp -f %q %q' "$target" "$source"
    fi
  else
    # TODO: Remove older backups
    _d_echo_eval_with_sudo 'mv %q{,.bak.%q}' "$target" "$(date +'%s')"
  fi
}

function _d_echo_eval_with_sudo() {
  local cmd=("$@")
  # TODO: Add sudo when it fails due to permissions
  # NOTE: If this gets migrated to Zsh, change 0 to 1
  # if fails with "...": cmd[0]="sudo ${cmd[0]}" & echo_eval ...
  echo_eval "${cmd[@]}"
}

function d_vim_setup() {
  if [[ ! ${VIM_PATH:-} || ${NO_VIM:-} || ! ${VIM_SPELL:-} || ! -d $VIM_SPELL ]]; then
    return 0
  fi

  local force=${1:-false} vim_plug_installed=${2:-true} vim_cmds=()

  if [[ $vim_plug_installed == false ]]; then
    vim_cmds+=('PlugInstall')
  fi

  local file files=()
  for file in "$VIM_SPELL"/*.add; do
    if [[ $force == true || ! $(find "$VIM_SPELL" -name "$(basename "$file")*.spl" -print -quit) ]]; then
      vim_cmds+=("mkspell! %q")
      files+=("$file")
    fi
  done

  if [[ ${#vim_cmds[@]} -eq 0 ]]; then
    return 0
  fi

  vim_cmds+=('q')
  echo_eval "vim +'$(join_array ' | ' "${vim_cmds[@]}")'" "${files[@]}"
}

function _d_log_error {
  local msg=$1 no_color=${2:-''}
  if [[ $no_color ]]; then
    echo >&2 "$msg"
  else
    color 'red' "$msg" >&2
  fi
}

# Allow customization and testing
HOME_DIR=${HOME_DIR:-$HOME}
ROOT_DIR=${ROOT_DIR:-'/'}
PLATFORM_PREFIX='platform_'

export DOTFILES_CMD
DOTFILES_CMD=$(basename "$0")

export VIM_SPELL=$HOME_DIR/.vim/spell

export EDITOR
export BUNDLER_EDITOR
export VIM_PATH
export VISUAL

export DOTFILES
DOTFILES="$(dirname "$(dirname "$(dirname "$(dirname "$(dirname "$(realpath "$0")")")")")")"
export DOTFILES_SHARED=$DOTFILES/shared
export DOTFILES_INCLUDES=$DOTFILES_SHARED/includes
export DOTFILES_CUSTOM=$DOTFILES/custom

# shellcheck disable=SC1091
. "$DOTFILES_INCLUDES"/lib/functions.sh
# shellcheck disable=SC1091
. "$DOTFILES_INCLUDES"/env.sh

USAGE_BASE="Usage: $(color 'green' "$DOTFILES_CMD")"

case "${1:-''}" in
  bootstrap) d_bootstrap ;;
  edit) d_edit ;;
  setup) d_bootstrap && d_setup ;;
  sync) d_sync "${2:-}" ;;
  update) d_update ;;
  import) d_import "${@:2}" ;;
  revert_import) d_revert_import "${@:2}" ;;
  revert_import_and_remove) d_revert_import_and_remove "${@:2}" ;;
  vim_setup) d_bootstrap && d_vim_setup true ;;
  *)
    _d_log_error "$USAGE_BASE <bootstrap|setup|sync|update|import|revert_import|revert_import_and_remove|vim_setup> ..." 1
    exit 1
    ;;
esac
