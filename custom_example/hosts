# Installation (might need redoing after a macOS update):
# sudo cp /etc/hosts{,.bak}
# sudo ln -f $DOTFILES_CUSTOM/hosts /etc
# Yes, should be a hard link (no -s) https://superuser.com/q/1311955/59919

##
# Host Database
#
# localhost is used to configure the loopback interface
# when the system is booting.  Do not change this entry.
##
127.0.0.1	localhost
255.255.255.255	broadcasthost
::1             localhost

# Modify DOCKER_HOST in env.sh accordingly
# Docker VM - `docker-machine ip`
# 192.168.99.100 docker # Docker Desktop
127.0.0.1 docker # Colima
