server {
  listen 80;
  # listen 443 ssl;
  server_name foo.local;

  location / {
    root       <%= ENV.fetch('CODE_ROOT') %>/foo;
    proxy_pass http://127.0.0.1:<%= ENV.fetch('FOO_PORT') %>;

    include includes/websockets.conf;
  }
}
