# cSpell:ignore fdns

alias aar='apt autoremove'
alias fm='free -h'
alias fdns='sudo systemd-resolve --flush-caches'

# Returns: `foo, bar`
function window_names() {
  # Implement for desktop Linux if needed
  :
}
