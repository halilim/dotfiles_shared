; Installation: install_php_conf (~/bin)

[global]
error_log = /usr/local/var/log/php-fpm.log
include = /private/etc/php-fpm.d/*.conf
; Throws `unknown entry 'daemonize'`, start as `php-fpm -F` instead
; daemonize = no
